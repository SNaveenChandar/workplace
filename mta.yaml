_schema-version: "3.2"
ID: poc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: poc-destination-content
  type: com.sap.application.content
  requires:
  - name: poc-destination-service
    parameters:
      content-target: true
  - name: poc-repo-host
    parameters:
      service-key:
        name: poc-repo-host-key
  - name: poc-uaa
    parameters:
      service-key:
        name: poc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: poc_html_repo_host
          ServiceInstanceName: poc-html5-srv
          ServiceKeyName: poc-repo-host-key
          sap.cloud.service: poc
        - Authentication: OAuth2UserTokenExchange
          Name: poc_uaa
          ServiceInstanceName: poc-xsuaa-srv
          ServiceKeyName: poc-uaa-key
          sap.cloud.service: poc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: poc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: poc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - poc.zip
      name: poc
      target-path: resources/
- name: poc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: poc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: poc-destination-service
    service-plan: lite
- name: poc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: poc-xsuaa-srv
    service-plan: application
- name: poc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: poc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
